config-file = ?custom

font-family = "0xProto"

theme = dankcolors

background-opacity = 0.96

scrollback-limit = 16777216
copy-on-select   = clipboard

shell-integration-features = no-cursor,sudo,title,no-ssh-env,no-ssh-terminfo

mouse-scroll-multiplier = 1

keybind = ctrl+shift+j=unbind
keybind = ctrl+shift+i=unbind
keybind = ctrl+,=unbind
