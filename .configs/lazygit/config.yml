gui:
  scrollHeight: 10
  language: "en"
  nerdFontsVersion: "3"

git:
  paging:
    colorArg: always
    pager: "delta --dark --paging=never --commit-style box --true-color always"
    # pager: "delta --dark --paging=never --commit-style box --true-color always -s --wrap-max-lines 0"

disableStartupPopups: true
