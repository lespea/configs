global vim
vimp = require('vimp')

vimp.add_chord_cancellations('n', '<leader>')

vimp.map_command("ShowAllMaps", (...) ->
  -- Use empty string as prefix to select all
  vimp.show_maps('', ...)
)

vimp.map_command("ShowMaps", (...) ->
  vimp.show_maps(...)
)
